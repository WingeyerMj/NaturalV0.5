
# Render Database URL (or local PG URL)
DATABASE_URL=postgresql://user:password@host:port/database

# Alternatively, manual DB connection vars
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=naturalfood

# Port for the server (Render sets this automatically)
PORT=10000

# Environment
NODE_ENV=production
