const XLSX = require('xlsx'); const wb = XLSX.readFile('Fuentes/Presupuestos/Prueba de Gral.xlsx'); console.log('Sheets:', wb.SheetNames); wb.SheetNames.forEach(name => { const ws = wb.Sheets[name]; const range = XLSX.utils.decode_range(ws['!ref'] || 'A1'); console.log('\n=== ' + name + ' === (Rows: ' + (range.e.r+1) + ', Cols: ' + (range.e.c+1) + ')'); const data = XLSX.utils.sheet_to_json(ws, {header:1, defval:''}); for(let i=0; i<Math.min(25, data.length); i++) { console.log('Row ' + i + ': ' + JSON.stringify(data[i].slice(0, 10))); } });
